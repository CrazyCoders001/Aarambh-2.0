public class Keyword {
    public static void main(String[] args) {
        // What are keywords ?
        // keywords are also known as data types  :- wrong statement

        // Keyword :- Keywords are predefined and reserved words in Java
        // Each keyword has a specific meaning and purpose in the language

        // int, float, char, double, boolean  :- it fits right
        // for, return, if, else, switch, case, break, continue etc.. :- it fits wrong

        // can i create a integer variable named as char ? :- No because char is a keyword
        char my_char = 'a';
        System.out.println(my_char);

        // Link to keyword list :- https://docs.oracle.com/javase/tutorial/java/nutsandbolts/_keywords.html
    }
}
